package com.example.music_base.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary: Neon Green (Signature Accent) ───────────────────────────────
val Primary = Color(0xFF72FE8F)           // Neon Green – main CTA & highlights
val PrimaryDim = Color(0xFF58E876)        // Slightly dimmed for pressed state
val PrimaryContainer = Color(0xFF0A3D1F)  // Dark green container (chip bg, etc.)
val OnPrimary = Color(0xFF003314)         // Text ON primary button
val OnPrimaryContainer = Color(0xFFA3FFB8) // Text inside primary container

// ─── Secondary: Soft Mint (harmony with Primary) ──────────────────────────
val Secondary = Color(0xFF9EF5B8)          // Soft mint – subtle accents
val SecondaryDim = Color(0xFF7DDEA0)
val SecondaryContainer = Color(0xFF0F4D2A)
val OnSecondary = Color(0xFF004D24)
val OnSecondaryContainer = Color(0xFFCCFFDD)

// ─── Tertiary: Electric Cyan (contrast accent) ────────────────────────────
val Tertiary = Color(0xFF5FD8F5)           // Cool cyan – for info/tags
val TertiaryDim = Color(0xFF3ABFDF)
val TertiaryContainer = Color(0xFF003D4D)
val OnTertiary = Color(0xFF003040)
val OnTertiaryContainer = Color(0xFFB8F0FF)

// ─── Background & Surface (near-black Obsidian) ───────────────────────────
val Background = Color(0xFF141414)         // Lighter Obsidian background
val Surface = Color(0xFF141414)
val SurfaceDim = Color(0xFF090909)
val SurfaceBright = Color(0xFF2A2A2A)
val SurfaceContainer = Color(0xFF161616)   // Card / bottom sheet bg
val SurfaceContainerLow = Color(0xFF111111)
val SurfaceContainerLowest = Color(0xFF000000)
val SurfaceContainerHigh = Color(0xFF1E1E1E)
val SurfaceContainerHighest = Color(0xFF252525)
val SurfaceVariant = Color(0xFF1C1C1C)     // Subtle layer on top of background

// ─── On Colors ────────────────────────────────────────────────────────────
val OnBackground = Color(0xFFF0F0F0)       // Slightly off-white (less harsh)
val OnSurface = Color(0xFFF0F0F0)
val OnSurfaceVariant = Color(0xFF9A9A9A)   // Cool neutral grey for secondary text

// ─── Outline ──────────────────────────────────────────────────────────────
val Outline = Color(0xFF555555)
val OutlineVariant = Color(0xFF2E2E2E)     // Subtle border / divider

// ─── Error ────────────────────────────────────────────────────────────────
val Error = Color(0xFFFF6B4A)
val ErrorDim = Color(0xFFCC4422)
val ErrorContainer = Color(0xFF5C1500)
val OnError = Color(0xFF3B0A00)
val OnErrorContainer = Color(0xFFFFCDBF)

// ─── Inverse ──────────────────────────────────────────────────────────────
val InverseSurface = Color(0xFFF2F2F2)
val InverseOnSurface = Color(0xFF1A1A1A)
val InversePrimary = Color(0xFF006B2A)

// ─── Surface Tint ─────────────────────────────────────────────────────────
val SurfaceTint = Color(0xFF72FE8F)

// ─── Share Sheet Colors ───────────────────────────────────────────────────
val ShareMint = Color(0xFFB4D0C8)
val ShareGreyGreen = Color(0xFF4A5D58)
val ShareDeepPurple = Color(0xFF8E44AD)
val ShareRoyalBlue = Color(0xFF2980B9)
val ShareOrange = Color(0xFFE67E22)
val ShareBlack = Color(0xFF121212)
